<nav>
    <ul>
        {{#foreach navigation}}
        <li class="nav-{{slug}} nav-item{{#if current}} nav-current{{/if}}">
          <a href="{{url absolute="true"}}">{{label}}</a>
        </li>
        {{/foreach}}
    </ul>
</nav>
