{{!< default}}
<div class="site-content">
  <div class="cards-header">
    <h1>{{tag.name}}</h1>
    {{#if tag.description}}
      <h2>{{tag.description}}</h2>
    {{/if}}
  </div>
  <div class="cards">
  {{#foreach posts}}
    {{> card-post}}
  {{/foreach}}
  </div>

  {{pagination}}
</div>
