<nav class="nav-wrapper">
    <ul class="nav-items">
        {{#foreach navigation}}
        <li class="nav-{{slug}} nav-item{{#if current}} nav-current{{/if}}">
          <h2 class="nav-item-title">
            <a class="nav-item-link" href="{{url absolute="true"}}">{{label}}</a>
          </h2>
        </li>
        {{/foreach}}
    </ul>
</nav>
